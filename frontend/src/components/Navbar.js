import React from 'react';

const navbar = () => {
    const navbarStyle = {
        // Add more styles as needed
    };
    return(
        <div style={navbarStyle}>
            navbar
        </div>
    )
    
};
export default navbar;